# Generated fonts

`Virgil-CJK.woff2` is generated from the Virgil and Xiaolai fonts bundled by
the locked `@excalidraw/excalidraw` dependency. Both source fonts permit
redistribution under the SIL Open Font License.

Run `pnpm fonts:build` after installing Python `fonttools` and `brotli`.
